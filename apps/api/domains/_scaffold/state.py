"""Typed state placeholder.

Copy this file into domains/<name>/state.py and replace with a TypedDict
(or dataclass) that your graph nodes read/write. Keep fields explicit —
avoid untyped `dict` state for new domains.
"""
