# README assets

This directory contains images that describe features already present in the repository.

`agentbridge-work-order-demo.png` is added in the final release task. Capture it from the Compose golden case, crop local paths, tokens, DSNs, user data, and unrelated windows, then update both README files in the same change.
