# AgentBridge 文档 P2（AI 可编程）Implementation Plan

> **For agentic workers:** 文风：白话、简明。实现后勾选。

**Goal:** 让 Cursor / Claude 等助手只读 `AGENTS.md` + `docs/ai-instructions/` 就能守住硬规则、加 echo 式插件、跑对测试命令。

**Architecture:** `AGENTS.md`/`CLAUDE.md` 双份内容一致作硬入口；`ai-instructions` 00～04 扩写为可执行手册；反模式并入 01/02；与 P1 `guide/` 交叉链接。

**Tech Stack:** Markdown only。

**Spec:** `docs/superpowers/specs/2026-07-28-docs-experience-design.md` §5

## Global Constraints

- 白话短句；术语必须跟一句解释
- 中文为主；路径/命令英文
- 产品名 AgentBridge；包名 `agentbridge_core` / `agentbridge-api`
- 冲突：完整方案 > guide/README > ai-instructions 摘要
- 不做单独 `05-do-not.md`
- 不重写完整方案 / 不大改 guide

---

### Task 1: AGENTS.md + CLAUDE.md

**Files:** `AGENTS.md`, `CLAUDE.md`, `.cursorrules`（若存在且过短则链到 AGENTS，不强行扩写）

- [x] 结构：一句话 → MUST 五条（各一句「违反会怎样」）→ 默认读序 → 禁止速查 → 验证命令
- [x] `CLAUDE.md` 与 `AGENTS.md` **正文一致**（可各有一行「与对方同步」说明，或完全相同）
- [x] 链到 `docs/ai-instructions/` 与 guide / 完整方案

### Task 2: ai-instructions 00～02

**Files:** `docs/ai-instructions/00-project-overview.md` … `02-domain-development.md`

- [x] 00：目录地图、三块、包名、权威优先级、读序
- [x] 01：MUST/禁止 + 违反会怎样 + lifespan 组装根 + 反模式
- [x] 02：加插件检查清单 + 常见错误 + 链 add-a-domain / guide/04

### Task 3: ai-instructions 03～04 + 导航

**Files:** `03-common-tasks.md`, `04-testing.md`, `docs/INDEX.md`（更新 AI 栏说明）

- [x] 03：起服务、加插件、external RAG、关键路径（可复制命令）
- [x] 04：测什么、`KNOWLEDGE_BACKEND=fake`、`AGENTBRIDGE_FAKE_RUNTIME`、命令
- [x] INDEX：去掉「下一阶段扩写」；标明 AI 手册已可用
- [x] 自检：`bash scripts/check_ai_instructions.sh`；抽查与 P1 术语无矛盾

## Done when

- [x] P2 验收清单（设计 §5.3）可勾选
